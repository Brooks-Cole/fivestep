from app import app

# This file is required for Vercel serverless functions
# It exposes the WSGI application used by Vercel's Python runtime
# No need to add app.run() here as Vercel manages the execution